form = '#form_add_acao';
var fdata = new FormData();

// add file ao append
var files = $ ('#anexo' )[0].files[0];
fdata.append( 'file', files );             

// adicionar inputs do formulario
$.each($(form).serializeArray(), function(i, field) {
    value[field.name] = field.value;
    fdata.append(field.name,field.value);
});      

$.ajax({
    type: 'POST',
    url: 'url',
    dataType: "json",  
    cache: false,
    contentType: false,
    processData: false,
    data: fdata,
    success: function(result) {
        console.log(result)
    }
});